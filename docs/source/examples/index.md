# Examples

Runnable scripts demonstrating `ocd_gd`, from a single orbit up to grid
sweeps. Each page below pulls its script in directly from `examples/` in the
repository, so the docs always match what actually runs.

```{toctree}
:maxdepth: 2

01_single_orbit_setup
02_single_orbit_chaos_check
03_batch_orbits
04_grid_basic
05_grid_chaos_map_plot
```
