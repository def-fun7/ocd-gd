version: 2

sphinx:
  configuration: docs/source/conf.py

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"

  # Use build jobs to explicitly tell the server how to install your package
  jobs:
    post_install:
      # Install your local package using pip without fetching its dependencies
      - pip install --no-deps .
